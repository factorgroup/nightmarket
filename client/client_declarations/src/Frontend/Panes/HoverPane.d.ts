import React from 'react';
/**
 * This is the pane that is rendered when you hover over a planet.
 */
export declare function HoverPane({ style, visible, element, }: {
    style?: React.CSSProperties;
    visible: boolean;
    element: React.ReactChild;
}): JSX.Element;
