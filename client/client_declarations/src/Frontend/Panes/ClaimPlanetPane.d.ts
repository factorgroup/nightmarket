import { LocationId } from '@darkforest_eth/types';
import React from 'react';
import { ModalHandle } from '../Views/ModalPane';
export declare function ClaimPlanetPane({ initialPlanetId, modal: _modal, }: {
    modal: ModalHandle;
    initialPlanetId?: LocationId;
}): React.ReactElement;
