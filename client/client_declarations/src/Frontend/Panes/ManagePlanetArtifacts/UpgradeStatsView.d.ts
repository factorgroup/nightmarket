/// <reference types="react" />
import { ArtifactType, Upgrade } from '@darkforest_eth/types';
export declare function UpgradeStatsView({ upgrade, isActive, artifactType, }: {
    upgrade: Upgrade;
    isActive: boolean;
    artifactType: ArtifactType;
}): JSX.Element;
